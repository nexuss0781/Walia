## Project Tasks

This repository’s active execution checklist lives in:

- `src/tooling/TASK.md`

Rationale: the tooling subsystem consumes task metadata, so we keep the canonical task plan close to it.

